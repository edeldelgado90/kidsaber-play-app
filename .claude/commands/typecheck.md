# Type Check

Runs TypeScript in strict mode and lists all type errors.

```bash
cd /Users/edelgado/Projects/kidsaber-play-app && npx tsc --noEmit
```

After running, report:
- Total number of type errors
- List each error with file path, line number, and message
- Suggest fixes for each error

If there are no errors, confirm the codebase is type-safe.
